trigger opportunityChatter on Custom_Chatter_Mapping__c (after insert) {

/*
FeedItem post = new FeedItem();

    for(Custom_Chatter_Mapping__c  m : Trigger.new) {
        if(Trigger.isInsert) {
            post.ParentId = m.Deal__c;
        }
    }
    insert post;*/
    
}