<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Property_Management_Required</fullName>
    <active>true</active>
    <description>If the Property Management Type is filled in, then the Property Manager field (called Assigned PM Company by property on the deal data tape) needs to be filled in.</description>
    <errorConditionFormula>AND(
ISBLANK( Property_Manager__c ),
ISCHANGED(Property_Management_Type__c),
NOT(ISNULL(TEXT(Property_Management_Type__c)))
)</errorConditionFormula>
    <errorDisplayField>Property_Manager__c</errorDisplayField>
    <errorMessage>Property Manager field (Assigned PM Company on the data tape) is required when the Property Management Type is filled in.</errorMessage>
</ValidationRule>
